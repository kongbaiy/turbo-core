# 目录结构规范

## 1. 应用层结构

### 主应用目录

```
apps/sc-cloud-basic/
├── src/
│   ├── api/                      # API 接口
│   │   ├── login/               # 登录相关 API
│   │   │   ├── index.ts
│   │   │   └── index.interface.ts
│   │   └── index.ts             # 统一导出
│   ├── assets/                   # 静态资源
│   │   └── images/              # 图片资源
│   │       ├── logo.png
│   │       ├── avatar.jpg
│   │       └── bg.jpg
│   ├── pages/                    # 页面组件
│   │   ├── home/                # 首页
│   │   │   ├── index.tsx
│   │   │   ├── index.module.scss
│   │   │   └── menu.tsx
│   │   └── login/               # 登录页
│   │       ├── account/         # 账号登录
│   │       │   ├── index.tsx
│   │       │   └── index.module.scss
│   │       ├── mobile/          # 手机登录
│   │       │   ├── index.tsx
│   │       │   └── index.module.scss
│   │       ├── index.tsx
│   │       └── index.module.scss
│   ├── router/                   # 路由配置
│   │   ├── async-component.tsx  # 异步组件
│   │   ├── auth.tsx             # 路由守卫
│   │   └── index.tsx            # 路由定义
│   ├── App.tsx                   # 根组件
│   └── main.tsx                  # 入口文件
├── index.html
├── package.json
└── vite.config.ts                # Vite 配置
```

### 子应用目录

```
apps/sc-cloud-platform/
├── src/
│   ├── api/                      # API 接口
│   │   ├── platform-manage/     # 平台管理 API
│   │   │   ├── index.ts
│   │   │   └── index.interface.ts
│   │   └── index.ts             # 统一导出
│   ├── assets/                   # 静态资源
│   ├── pages/                    # 页面组件
│   │   └── platform-manage/     # 平台管理页面
│   │       ├── index.tsx
│   │       ├── index.module.scss
│   │       └── dialog/          # 弹窗组件
│   │           └── form.tsx
│   ├── router/                   # 路由配置
│   │   ├── async-component.tsx
│   │   ├── auth.tsx
│   │   └── index.tsx
│   ├── App.tsx
│   └── main.tsx
├── index.html
├── package.json
└── vite.config.ts
```

## 2. 共享包结构

### 主题包

```
packages/antd-theme/
├── index.ts                      # 主入口
├── theme.ts                      # 主题配置
├── reset.scss                    # 样式重置
└── package.json
```

### 配置包

```
packages/config/
├── eslint/                       # ESLint 配置
│   ├── src/
│   │   ├── base.ts
│   │   ├── index.ts
│   │   ├── next.ts
│   │   └── react-internal.ts
│   ├── build.config.ts
│   └── package.json
├── stylelint/                    # Stylelint 配置
│   ├── src/
│   │   └── index.ts
│   ├── build.config.ts
│   └── package.json
├── typescript/                   # TypeScript 配置
│   ├── base.json
│   ├── nextjs.json
│   ├── package.json
│   └── react-library.json
└── vite/                         # Vite 配置
    ├── src/
    │   ├── index.ts
    │   └── proxy.ts
    ├── build.config.ts
    └── package.json
```

### 组件包

```
packages/react-components/
├── button/                       # 按钮组件
│   ├── index.tsx
│   ├── index.ts
│   └── button.module.scss
├── error-boundary/               # 错误边界
│   ├── index.tsx
│   ├── index.module.css
│   └── index.ts
├── index.ts                      # 统一导出
└── package.json
```

### Hooks 包

```
packages/react-hooks/
├── index.ts                      # 导出所有 Hooks
├── package.json
└── tsconfig.json
```

### 工具包

```
packages/utils/
├── src/
│   ├── antd-global.ts            # Antd 全局配置
│   ├── axios.ts                  # Axios 封装
│   ├── sha256.ts                 # SHA256 加密
│   └── storage.ts                # 存储管理
├── index.ts                      # 导出所有工具
├── package.json
└── tsconfig.json
```

## 3. 类型定义

### 全局类型

```
types/
├── axios.d.ts                    # Axios 类型扩展
├── css-modules.d.ts              # CSS Modules 类型
└── global.d.ts                   # 全局类型
```

### API 类型

```
src/api/[模块]/index.interface.ts
```

## 4. 配置文件

### 根配置

```
/
├── .editorconfig                 # 编辑器配置
├── .env.development              # 开发环境变量
├── .env.production               # 生产环境变量
├── .gitignore                    # Git 忽略文件
├── .npmrc                        # npm 配置
├── .prettierrc.json              # Prettier 配置
├── .stylelintignore              # Stylelint 忽略文件
├── commitlint.config.ts          # Commitlint 配置
├── eslint.config.ts              # ESLint 配置
├── monorepo.code-workspace       # VS Code 工作区
├── package.json                  # 根 package.json
├── pnpm-lock.yaml                # pnpm 锁文件
├── pnpm-workspace.yaml           # pnpm workspace 配置
├── stylelint.config.ts           # Stylelint 配置
├── tsconfig.json                 # TypeScript 配置
├── turbo.json                    # TurboRepo 配置
└── uno.config.ts                 # UnoCSS 配置
```

## 5. Git Hooks

```
.husky/
├── commit-msg                    # Commit 消息检查
└── pre-commit                    # 提交前检查
```
