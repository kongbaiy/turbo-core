export default function App() {
  return (
    <main className="container">
      <h1>React 18 Demo</h1>
      <p>This is a standalone React 18 app inside the existing monorepo.</p>
      <p>Run <code>pnpm --filter react-demo dev</code> to start it.</p>
    </main>
  );
}
